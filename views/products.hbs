{{#extend "stylesheets"}}
<link rel="stylesheet" href="/stylesheets/product-list.css" />
{{/extend}}
    
<h1>Product List</h1>
<div class="product-list">
    {{#each products}}
        <div class="product-list-element">
            <a href="/product/{{this.product_name}}" ><h3 class="capitalized">{{this.product_name}}</h3></a>
            <a class="add-wishlist" href="/add-wishlist/{{this._id}}" type="button">❤️</a>
            <h4 class="capitalized">Brand: {{this.brands}}</h4>
            <img src="{{this.image_url}}">
            {{#ifCond this.nutrition_grades_tags}}
                <h4>🤷‍♂️</h4>
            {{else}}
                <img src="/images/nutriscore-{{this.nutrition_grades_tags}}.png">
            {{/ifCond}}

        </div>
    {{/each}}
</div>

{{!-- Be able to show the correct image depending on the nutriscore  --}}

